---
title: Session capture 2026-09-26 (settled)
type: raw-source
source: session
collected: 2026-09-26
sha256: 87df461581cfd6179b491366af946d8d934a2749fd74bb9793b749ce1a1304a8
---

### Documentation ingest (wiki_ingest) must run with extension discovery disabled via `pi -ne -e <packed>/src/extension.ts` to properly test the packed artifact in isolation from installed extensions.
Kind: procedure
Evidence:
- file: docs/wiki/wiki/pi/one-install-source.md — "A packed-artifact smoke test must run with extension discovery disabled (`pi -ne -e <packed>/src/ext..."
Verdict: file (grounded 0.80, derivable 0.29, importance 0.52, criticality 0.31)
Target: pi/one-install-source.md
Reinforced: pi/one-install-source.md#c2 (3×)

### pi's npm store pins a `^<minor>` dependency range, and for 0.x packages that excludes the next minor (`^0.7.1` does not match `0.8.0`), so `pi update --extension npm:<pkg>@latest` can report success while the store stays on the old minor version; an explicit-version update rewrites the spec and succeeds.
Kind: gotcha
Evidence:
- file: docs/RELEASING.md — "pi's npm store pins a `^<minor>` range, and for 0.x releases that excludes the next minor (`^0.7.1` does not match `0.8.0`),"
- command: pi update --extension npm:pi-jev-wiki@latest (reported success but installed 0.7.1)
Verdict: file (grounded 0.97, derivable 0.34, importance 0.57, criticality 0.25)
Target: pi/npm-minor-version-pin.md
Reinforced: pi/npm-minor-version-pin.md#c1 (3×)

### Install only one source of a pi extension at a time; registering two copies of the extension (for example the npm package and a local path) causes the extension to be discovered twice and behaved incorrectly.
Kind: invariant
Evidence:
- file: README.md — "Install **one source at a time**. Registering two copies of the extension (for example the npm package *and* a local..."
Verdict: file (grounded 0.96, derivable 0.40, importance 0.72, criticality 0.25)
Target: pi/one-install-source.md
Reinforced: pi/one-install-source.md#c1 (4×)

### CI in pi-jev-wiki publishes only to npm (with SLSA provenance via the publish.yml workflow); the GitHub release is created manually from the changelog section, and a fresh npm version may lag a minute or two before `npm view` observes it.
Kind: procedure
Evidence:
- file: docs/RELEASING.md
Verdict: file (grounded 0.95, derivable 0.36, importance 0.50, criticality 0.27)

### Review items that belong to a different wiki's subject should be resolved with disposition `out_of_scope` and a recorded `target` wiki, which declines the item rather than marking it wrong; this is how misfiled captures that land in the wrong wiki queue are cleared.
Kind: pattern
Evidence:
- command: wiki_review resolve 16 home-wiki items as out_of_scope with target pi-jev-wiki
Verdict: review (grounded 0.76, derivable 0.42, importance 0.57, criticality 0.35)

### The home life-wiki (C:/Users/liand/docs/wiki) is not under version control; the pipeline's ledger/log/TOC are its record of record.
Kind: fact
Evidence:
- command: git -C C:/Users/liand rev-parse --is-inside-work-tree (not a git repo)
Verdict: reject_derivable (grounded 0.71, derivable 0.63, importance 0.57, criticality 0.42)

### The round-trip of applyReviewResolution(accept) rewrites a page through the serializer and must be re-validated; the behavior is documented in SKILL.md, but the code check is not yet implemented, leaving an open correctness gap in a path that rewrites frontmatter.
Kind: gotcha
Evidence:
- file: skills/llm-wiki/SKILL.md
Verdict: reject_unsupported (grounded 0.61, derivable 0.22, importance 0.70, criticality 0.54)

### When updating an existing wiki hub page with new preferences, prefer merging into the existing hub (e.g. collaboration/gotcha-working-style.md) rather than filing new pages, because that is where agents actually read them.
Kind: pattern
Verdict: reject_unsupported (grounded 0.03, derivable 0.13, importance 0.38, criticality 0.26)
