---
title: Session capture 2026-09-20 (settled)
type: raw-source
source: session
collected: 2026-09-20
sha256: 8fd2ef085c2fc593d86bfec80e229099ae105f931a55c0b9a4e2612056456c6a
---

### Releases in this repo are two commits: a `docs(skill)`/feature commit followed by a `chore(release): X.Y.Z` commit that bumps version and creates the annotated tag; a third `docs(release)` commit then records the release in README and docs/RELEASING.md.
Kind: procedure
Evidence:
- commit: f78497d — "chore release 0.3.0"
- commit: 4f57a2b — "docs(release): record ... release"
Verdict: reject_unsupported (grounded 0.35, derivable 0.34, importance 0.42, criticality 0.19)

### `npm version` refuses to run when the git tree is dirty, even if changes are only staged, so the release flow must bump without commit/tag (`npm version minor --no-git-tag-version`-style) and make the release commit and tag explicitly.
Kind: gotcha
Evidence:
- command: npm version minor -m "chore(release): %s"
Verdict: reject_unsupported (grounded 0.10, derivable 0.26, importance 0.47, criticality 0.39)

### Publishing is triggered by pushing a version tag; GitHub Actions builds and publishes pi-jev-wiki to npm with SLSA provenance, so the tag push is the complete release path.
Kind: architecture
Evidence:
- command: git push origin v0.4.0
- command: npm view pi-jev-wiki@0.4.0 version
Verdict: reject_unsupported (grounded 0.10, derivable 0.26, importance 0.37, criticality 0.54)

### No GitHub releases exist for this repo (0.2.0 and 0.3.0 were tag + CI only), and there is no `gh` CLI or GitHub token in the environment, so creating a GitHub release page is a manual step outside the normal release flow.
Kind: fact
Evidence:
- command: curl releases API returned HTTP 200 with empty list
Verdict: reject_unsupported (grounded 0.50, derivable 0.32, importance 0.26, criticality 0.20)

### The packed-artifact smoke test must run isolated with `pi -ne -e …` because a globally installed pi-jev-wiki conflicts with the packed duplicate.
Kind: gotcha
Evidence:
- command: pi -ne -e ··· ; global install conflicts with the packed duplicate
Verdict: review (grounded 0.88, derivable 0.47, importance 0.31, criticality 0.28)

### The llm-wiki skill is the de facto schema for claim records: frontmatter supports only flat scalars, inline arrays, and lists of flat maps, and block-style nested `evidence:` lists corrupt serialization by expanding into the root `claims` array (or char-by-char).
Kind: invariant
Evidence:
- file: src/wiki/frontmatter.ts
- file: skills/llm-wiki/SKILL.md
Verdict: review (grounded 0.81, derivable 0.50, importance 0.95, criticality 0.65)

### Version history must be preserved rather than rewritten: when a previously filed wiki claim becomes false, supersede it by keeping the original text and adding `superseded_by`/`superseded_at` plus a resolution note; never silently rewrite or delete.
Kind: decision
Evidence:
- file: docs/wiki/wiki/architecture/gotcha-skill-schema-drift.md
Verdict: reject_unsupported (grounded 0.60, derivable 0.21, importance 0.91, criticality 0.60)

### Skill files should stay short by deferring: move setup to a single pointer to `wiki_setup` and thresholds to a pointer to `wiki_triage` rather than inlining walkthroughs.
Kind: pattern
Evidence:
- file: skills/llm-wiki/SKILL.md
Verdict: reject_unsupported (grounded 0.35, derivable 0.19, importance 0.56, criticality 0.29)
