---
title: Session capture 2026-09-26 (settled)
type: raw-source
source: session
collected: 2026-09-26
sha256: 541640c23df2f8e77bfbcae5ca64d2acb3e87298668f0ec32e2fc7c7cd543ccd
---

### The `wiki` parameter for cross-wiki writes should be implemented as a single shared target resolver (`resolveTargetRuntime(ctx, wikiName?)`) used by all write tools, mirroring the existing `wiki_index` parameter semantics (registered name, one wiki per call, default unchanged).
Kind: decision
Evidence:
- file: handoffs/2026-09-26-cross-wiki-write-tools.md — "one shared target resolver (`resolveTargetRuntime(ctx, wikiName?)`) used by all 7 write tools, matching `wiki_index`'s existing `wiki` param"
Verdict: review (grounded 0.78, derivable 0.27, importance 0.65, criticality 0.50)

### When a target wiki is specified for a write tool, `resolvePagePath` and `wiki_ingest`'s path anchoring must resolve against the target project root, never `ctx.cwd`; likewise `wiki_sync` with a target must diff the target project's git repo, not the session's.
Kind: gotcha
Evidence:
- file: handoffs/2026-09-26-cross-wiki-write-tools.md — "anchor to the **target** project root when `wiki` is given (never `ctx.cwd`)"
Verdict: reject_unsupported (grounded 0.64, derivable 0.26, importance 0.71, criticality 0.72)

### Cross-wiki target resolution and path logic belong in a small standalone module so they are unit-testable offline without the pi runtime.
Kind: pattern
Evidence:
- file: handoffs/2026-09-26-cross-wiki-write-tools.md — "resolveTargetRuntime"
Verdict: reject_unsupported (grounded 0.20, derivable 0.20, importance 0.57, criticality 0.39)

### `review.maxPerSession` was only ever used as the `wiki_review` display cap; no budget semantics exist in the code, so listing limits should use a proper `limit` param instead of overloading that setting.
Kind: gotcha
Evidence:
- file: src/extension.ts — "`review.maxPerSession` (default 10) is used only as the `wiki_review` display cap (`src/extension.ts:1454`)"
Verdict: file (grounded 0.17, derivable 0.41, importance 0.53, criticality 0.40)

### Ingest hashing must normalize CRLF to LF before both hashing and storing, otherwise Windows line endings break dedup/raw-index matching.
Kind: gotcha
Evidence:
- file: handoffs/2026-09-26-cross-wiki-write-tools.md — "normalize `\r\n`→`\n` before hashing *and* storing; one-line hazard for Windows users"
Verdict: reject_unsupported (grounded 0.62, derivable 0.42, importance 0.68, criticality 0.59)

### The planned 0.8.0 scope includes a `wrong_topic` resolution that records the decline plus a suggested target in the ledger and leaves the page untouched; the naming choice (`wrong_topic` vs `out_of_scope`) affects the documented vocabulary in SKILL.md, README, and `wiki_review` output.
Kind: decision
Evidence:
- file: handoffs/2026-09-26-cross-wiki-write-tools.md — "add `wrong_topic` resolution: records the decline + suggested target in the ledger and leaves the page alone"
Verdict: reject_unsupported (grounded 0.14, derivable 0.15, importance 0.41, criticality 0.40)

### For 10.1 capture routing, the recommended design is to auto-route only when evidence points unambiguously at exactly one other registered wiki, otherwise stay on the session wiki and emit a visible mismatch warning in the capture brief (optional `capture.route: session | subject` knob).
Kind: decision
Evidence:
- user: Assistant recommendation awaiting Andrei's call — "route automatically when the evidence points unambiguously at exactly one other registered wiki, otherwise stay on the session wiki and put a visible mismatch warning"
Verdict: file (grounded 0.87, derivable 0.31, importance 0.53, criticality 0.44)

### `SKILL.md` is the schema contract for the wiki — frontmatter/format behaviors (e.g. scalar normalization on round-trip) must be documented there, not only in code.
Kind: invariant
Evidence:
- file: skills/llm-wiki/SKILL.md — "the skill is the schema contract"
Verdict: file (grounded 0.94, derivable 0.48, importance 0.98, criticality 0.59)
Target: architecture/gotcha-skill-schema-drift.md
