---
title: Session capture 2026-09-26 (tool)
type: raw-source
source: session
collected: 2026-09-26
sha256: 525a8aeb35d5a0c5bfc7c6d2fad438ee7160dc197edaffa3d1aa1452541cdfde
---

### The local embedding model emits progress callbacks while loading from cache, not only on first download — the quality preset produced 182 events in 1.3 s — so any progress sink that forwards each event floods the terminal.
Kind: gotcha
Evidence:
- file: src/vector/embed.ts — "options.onProgress?.(`${info.file ?? "model"} ${Math.round(info.progress)}%`)"
- command: npx jiti scripts/progress-check.ts — "progress events: 182 in 1315ms (quality preset, model already cached)"
Verdict: file (grounded 0.57, derivable 0.44, importance 0.46, criticality 0.28)

### Embedding-model load progress is shown as one throttled footer status entry (ctx.ui.setStatus with key "jev-wiki", 250 ms throttle, always cleared in a finally) instead of console output, because tool console lines accumulate in the transcript; without a UI it prints one stable line.
Kind: decision
Evidence:
- file: src/extension.ts — "modelProgressSink: ctx.ui.setStatus(key, text) throttled to 250 ms, cleared in done(); console.error once when !ctx.hasUI"
- file: src/vector/index.ts — "onProgress?: (message: string) => void (IndexOptions)"
Verdict: reject_derivable (grounded 0.82, derivable 0.69, importance 0.44, criticality 0.11)

### writeIndex prunes the per-topic shards under toc/ for topics with no entries left; before this, removing the last page of a topic (e.g. wiki_remove) left a stale toc/<topic>.md listing deleted pages while index.md and toc.md were correct.
Kind: invariant
Evidence:
- file: src/wiki/toc.ts — "Prune shards whose topic has no entries left, so removing the last page of a topic cannot leave a stale toc/<topic>.md behind."
- test: scripts/unit-test.ts — "ok prunes topic shards whose last page was removed"
Verdict: review (grounded 0.95, derivable 0.83, importance 0.66, criticality 0.23)
