---
title: Session capture 2026-09-26 (tool)
type: raw-source
source: session
collected: 2026-09-26
sha256: 786bb0c802d7af5ef097852529b1d383489012002fa9b4c439b5b3c48b721705
---

### pi-jev-wiki must be installed from exactly one source at a time — npm package, local folder, or git — because registering two copies makes pi refuse to load the extension with 'Tool "wiki_*" conflicts with …': every tool name is registered twice.
Kind: gotcha
Evidence:
- file: README.md — "Install **one source at a time**. Registering two copies of the extension (for example the npm package *and* a local folder) makes pi refuse to load it with `Tool "wiki_*" conflicts with …`, because every tool name is registered twice. Check with `pi list`; drop the duplicate with `pi remove <source>`."
- commit: df0989d — "docs(readme): warn about duplicate extension installs"
Verdict: file (grounded 0.91, derivable 0.39, importance 0.59, criticality 0.26)

### The legacy `capture.onSettle: true` alias predates `capture.cadence` and still enables task capture, so a plugin version older than 0.7.0 (e.g. the 0.4.0 install in WSL) can run task-cadence capture by carrying the alias before it is upgraded.
Kind: pattern
Evidence:
- file: docs/DESIGN.md — "The legacy `capture.onSettle: true` still enables task capture, and `capture.onCompact: true` stays an independent pre-compaction trigger."
- file: //wsl.localhost/Ubuntu/home/liand/.pi/agent/jev-wiki.json — "{ "capture": { "cadence": "task", "onSettle": true } }"
- commit: b13904a — "feat(wiki): P0 walking skeleton with Jev-verified ingest and capture (initial source of onSettle)"
Verdict: reject_unsupported (grounded 0.33, derivable 0.26, importance 0.45, criticality 0.29)
