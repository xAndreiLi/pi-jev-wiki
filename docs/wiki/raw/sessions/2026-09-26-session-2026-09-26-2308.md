---
title: Session capture 2026-09-26 (settled)
type: raw-source
source: session
collected: 2026-09-26
sha256: aec0159683d241fd2bc9d1628afb4ae3e3b60ef852b3dd9218afc954fce8df7a
---

### In pi-jev-wiki, insight evidence refs (file/commit kinds) are resolved relative to the process cwd, not the registered project root; if the capture session runs outside a git repo or outside the project directory, file and commit refs resolve to "not found" and Jev grounds those claims at 0.03–0.05 instead of the ~0.9 seen with in-repo sessions.
Kind: gotcha
Evidence:
- file: src/extension.ts — "buildInsightEvidence (src/extension.ts:108)"
- command: grep -n -A 25 "8.1 Capture cadence" docs/DESIGN.md
- user: The real cause is cwd-relative resolution
Verdict: file_user_stated (grounded 0.22, derivable 0.28, importance 0.49, criticality 0.51)

### pi-jev-wiki's file-kind evidence validation ignores its own `quote` field when the referenced file cannot be read, and the extraction prompt makes quotes optional while tool results never reach the capture transcript — so the extractor usually cannot quote file contents, making file-backed claims unverifiable from foreign cwd sessions.
Kind: gotcha
Evidence:
- file: src/extension.ts — "kind: "file" ignores its own quote when the file can't be read"
Verdict: reject_unsupported (grounded 0.30, derivable 0.24, importance 0.51, criticality 0.46)

### pi-jev-wiki must be installed from exactly one source at a time (npm, local folder, or git); registering two copies makes pi silently mis-resolve the extension and/or refuse to load at session start.
Kind: invariant
Evidence:
- file: docs/wiki/wiki/pi/one-install-source.md
- commit: df0989d
- user: pi-jev-wiki must be installed from exactly one source at a time — npm package, local folder, or git — because registering two copies makes pi refuse to load the extension
Verdict: file (grounded 0.87, derivable 0.39, importance 0.76, criticality 0.35)
Target: pi/one-install-source.md
Reinforced: pi/one-install-source.md#c1 (2×)

### The user's capture/agent judging pipeline (Jev) correctly rejects forward-compat extrapolations that a source passage does not literally support — e.g. a DESIGN passage about the `onSettle` alias enabling task capture does not license the claim that older 0.4.0 installs carry the alias before upgrade.
Kind: pattern
Evidence:
- user: Jev rejected it as ungrounded — correctly: the DESIGN passage supports "the alias enables task capture", not the extrapolation
Verdict: reinforce (grounded 0.87, derivable 0.20, importance 0.55, criticality 0.34)
Target: architecture/flow-capture.md

### The capture cadence semantics and the legacy `onSettle` alias are already documented in `architecture/flow-capture.md` (claims c2 and c4); future agents should not re-file them.
Kind: fact
Evidence:
- file: docs/wiki/wiki/architecture/flow-capture.md
Verdict: reject_derivable (grounded 0.95, derivable 0.58, importance 0.85, criticality 0.19)

### A single capture session can concern multiple wikis (e.g. a pi-jev-wiki repo session whose pages were misplaced into the life wiki); routing must therefore be per-claim (by evidence prefix against each wiki's projectRoot), not one-target-per-capture, to avoid silently mis-filing mixed sessions.
Kind: decision
Evidence:
- user: Split granularity — recommended: per-claim routing. Claims group by evidence prefix against each wiki's projectRoot; one capture can update two wikis.
Verdict: reinforce (grounded 0.91, derivable 0.27, importance 0.72, criticality 0.61)
Target: invariants/wiki-layout-atomic-root.md

### In pi-jev-wiki's roadmap, Phase 1 (root-aware evidence resolution, `wiki:` targeting, suggested target in the pending-capture note) should ship as an independent release before Phase 2 (rewrite of capture core: per-claim routing, per-wiki adjudication, `capture.route` ledger, catalog roots), so Phase 1's evidence fix can be observed in production before the core rewrite doubles the diff.
Kind: decision
Evidence:
- user: Phase 1 first, Phase 2 as the next release
- file: docs/plans/PLAN.md
Verdict: reject_unsupported (grounded 0.52, derivable 0.18, importance 0.52, criticality 0.44)
