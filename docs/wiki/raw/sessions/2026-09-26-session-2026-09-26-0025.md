---
title: Session capture 2026-09-26 (tool)
type: raw-source
source: session
collected: 2026-09-26
sha256: 2713837d7905ce8187e22710dacdffd86b590f7186ba6c13c7ff5fc25c726834
---

### Cross-wiki writes are explicit and per-call: every write tool takes `wiki: "<registered name>"`, targets exactly one registered wiki per call, and keeps the session's wiki as the default; relative page and ingest paths resolve against the target project rather than the session workspace, and `wiki_sync wiki=<name>` diffs the target project's repository.
Kind: decision
Evidence:
- commit: c5e5f62
- user: Andrei, 2026-09-26: "Lets go with your reccommendations here. I am okay to scope this all in one batch to release as a minor version bump. Let's plan with cross wiki writes in mind and implement that first. Then the auto routing can work by default and warn if it is ambiguous." — "Lets plan with cross wiki writes in mind and implement that first."
Verdict: review (grounded 0.91, derivable 0.53, importance 0.81, criticality 0.67)

### Auto-capture routes by subject by default: `capture.route: subject` files the capture into the registered wiki that owns the files the session actually edited when exactly one does; ambiguous, missing, or evidence-only matches keep the session wiki and add a visible warning to the brief. `capture.route: session` restores the working-directory rule.
Kind: invariant
Evidence:
- commit: c5e5f62
- user: Andrei, 2026-09-26: "Let's plan with cross wiki writes in mind and implement that first. Then the auto routing can work by default and warn if it is ambiguous." — "Then the auto routing can work by default and warn if it is ambiguous."
Verdict: review (grounded 0.83, derivable 0.52, importance 0.75, criticality 0.45)

### A `user`-kind evidence item that does not appear in an actual user turn is now presented to Jev as agent-stated (unverified), so the agent's own recommendation cannot win the user_stated trust tier — the mitigation for the capture-files-proposals gotcha.
Kind: gotcha
Evidence:
- commit: c5e5f62
- file: docs/wiki/wiki/architecture/gotcha-capture-proposals.md
Verdict: reject_derivable (grounded 0.93, derivable 0.64, importance 0.77, criticality 0.55)

### Raw sources are append-only: a same-day/same-title ingest or same-minute session capture gets a content-hash suffix instead of overwriting the earlier file, and ingest normalizes CRLF/CR to LF before hashing and storing so Windows line endings cannot break dedup or raw-index matching.
Kind: invariant
Evidence:
- commit: c5e5f62
Verdict: review (grounded 0.92, derivable 0.61, importance 0.82, criticality 0.63)

### `out_of_scope` is the review disposition for a claim that is correct but belongs to another wiki: it records the resolution with an optional `target` and leaves the page frontmatter untouched, so the claim is not marked wrong and does not feed rejection calibration.
Kind: decision
Evidence:
- commit: c5e5f62
- user: Andrei, 2026-09-26, approving the recommendations: "Lets go with your reccommendations here." — "Lets go with your reccommendations here."
Verdict: file (grounded 0.57, derivable 0.37, importance 0.66, criticality 0.47)
