---
title: Session capture 2026-09-26 (settled)
type: raw-source
source: session
collected: 2026-09-26
sha256: f59c58f61fe07591805d063a55ef3fb80cbb4afdb0109c98410bf7f38389b361
---

### The local embedding model emits roughly 180 progress events in about 1.3 seconds even when the model is loaded from cache, so any progress reporting must be throttled rather than assume only the cold download path floods.
Kind: gotcha
Evidence:
- file: src/vector/embed.ts — "options.onProgress?.(`${info.file ?? "model"} ${Math.round(info.progress)}%`)"
- command: npx jiti scripts/progress-check.ts
Verdict: file_user_stated (grounded 0.14, derivable 0.15, importance 0.41, criticality 0.36)

### Model-load progress must be surfaced through the pi extension host's one-line footer status (ctx.ui.setStatus) rather than console output, because console writes from a tool execution produce unusable floods.
Kind: pattern
Evidence:
- file: src/extension.ts
- user: So the one-line animation you wanted is possible — no removal needed.
Verdict: reject_derivable (grounded 0.72, derivable 0.72, importance 0.52, criticality 0.23)

### Non-interactive pi modes (print/JSON) should emit exactly one stable status line instead of transient percentage updates, since there is no footer to animate.
Kind: decision
Evidence:
- file: src/extension.ts — "[jev-wiki] embedding model: loading..."
Verdict: reject_derivable (grounded 0.87, derivable 0.70, importance 0.48, criticality 0.12)

### toc/<topic>.md shards are agent-facing TOC files that must be pruned when a topic loses its last entry; writeIndex previously only wrote shards for surviving topics, leaving stale shards that broke the single-writer no-drift invariant.
Kind: invariant
Evidence:
- file: src/wiki/toc.ts
- test: scripts/unit-test.ts — "prunes topic shards whose last page was removed"
Verdict: review (grounded 0.77, derivable 0.48, importance 0.71, criticality 0.48)

### pi-jev-wiki releases are performed with `npm run release -- <version>`, which commits the changelog section, tags the release, and relies on the publish.yml GitHub Actions workflow to publish to npm with SLSA provenance.
Kind: procedure
Evidence:
- file: docs/RELEASING.md
- command: npm run release -- 0.7.1
Verdict: file (grounded 0.94, derivable 0.47, importance 0.57, criticality 0.38)

### The 0.2.0 release was published manually and has no SLSA attestation; 0.3.0 onward were CI-published with provenance, so provenance checks must account for the missing 0.2.0 attestation.
Kind: gotcha
Evidence:
- file: docs/RELEASING.md — "0.2.0 was the manual first release and has no attestation"
Verdict: file (grounded 0.94, derivable 0.46, importance 0.54, criticality 0.52)

### On Windows, `gh` is not on PATH by default; it must be invoked with PATH extended to '/c/Program Files/GitHub CLI' for release verification steps.
Kind: gotcha
Evidence:
- command: export PATH="/c/Program Files/GitHub CLI:$PATH" && gh run list --workflow publish.yml
Verdict: reject_unsupported (grounded 0.69, derivable 0.35, importance 0.31, criticality 0.22)

### Because the fix ships only when a new version is installed and a new session starts, an already-running session keeps executing the previously installed version and still exhibits the bug.
Kind: gotcha
Evidence:
- user: this session still runs the installed 0.7.0, so you'll keep seeing the old flood until a release is installed and a new session starts
Verdict: file (grounded 0.95, derivable 0.36, importance 0.35, criticality 0.24)
Target: invariants/extension-loads-at-session-start.md
