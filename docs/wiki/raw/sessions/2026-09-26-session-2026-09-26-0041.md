---
title: Session capture 2026-09-26 (tool)
type: raw-source
source: session
collected: 2026-09-26
sha256: 53b6eff00e41fcfcbc2d46492d545b3e2ccd0924cd54730afae96f2592d63db7
---

### An npm-installed extension does not cross minor versions through `pi update --extension <source>@latest` alone: pi's npm store records a `^<minor>` dependency range and for 0.x releases that excludes the next minor (`^0.7.1` does not match `0.8.0`), so the update reports success while the installed copy stays old; installing the explicit version rewrites the spec to `^0.8.0`.
Kind: gotcha
Evidence:
- file: docs/RELEASING.md — "pi's npm store pins a `^<minor>` range, and for 0.x releases that excludes the next minor (`^0.7.1` does not match `0.8.0`), so `pi update --extension npm:pi-jev-wiki@latest` can report success while staying on the old version."
- command: pi update --extension npm:pi-jev-wiki@latest → "Updated npm:pi-jev-wiki@latest" but the store copy's package.json still read 0.7.1; pi update --extension npm:pi-jev-wiki@0.8.0 rewrote the spec to ^0.8.0 and installed 0.8.0.
Verdict: file (grounded 0.97, derivable 0.29, importance 0.57, criticality 0.27)
