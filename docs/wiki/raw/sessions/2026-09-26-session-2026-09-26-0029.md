---
title: Session capture 2026-09-26 (settled)
type: raw-source
source: session
collected: 2026-09-26
sha256: 51ef9eefdfb23489d8fe513c0608531561a4bb3c39dbf77b79cd00ea40776aa1
---

### The serializer round-trip is asymmetric: parseFrontmatter leaves exactly one separator newline, and the recorded raw-source key is the pre-serialization text which may or may not carry a trailing newline — so verifying raw-source hashes must reverse the serializer (strip one leading newline + trimEnd) and accept both exact and trailing-trimmed forms, otherwise nearly every real source false-positives.
Kind: gotcha
Evidence:
- file: src/grounding.ts — "Reverse the serializer exactly: it inserts one blank line after the frontmatter and appends a final newline"
- command: npm run test:unit
Verdict: reject_unsupported (grounded 0.04, derivable 0.17, importance 0.43, criticality 0.61)

### Cross-wiki writes are exposed through an explicit per-call `wiki` parameter on the write tools rather than being inferred, and path resolution is anchored to the target wiki's project root so a cross-wiki write cannot fall back to a same-named file in the session workspace.
Kind: decision
Evidence:
- commit: c5e5f62
Verdict: review (grounded 0.88, derivable 0.57, importance 0.73, criticality 0.63)

### Auto-capture routing defaults to the subject wiki — the registered wiki that owns the session's edited files — when exactly one does, and warns and falls back to the session's working-directory wiki on ambiguity.
Kind: decision
Evidence:
- file: src/config.ts — ""capture": { "cadence": "manual", "onCompact": false, "route": "subject" }"
- commit: c5e5f62
Verdict: review (grounded 0.78, derivable 0.44, importance 0.68, criticality 0.40)

### Claims that are correct but belong to another wiki are resolved with an `out_of_scope` review disposition carrying an optional target wiki, rather than being rejected or forced into the current wiki.
Kind: decision
Evidence:
- file: docs/wiki/wiki/decisions/out-of-scope-disposition.md
- commit: 0ff0465
Verdict: file (grounded 0.97, derivable 0.46, importance 0.90, criticality 0.43)
Target: decisions/out-of-scope-disposition.md

### The release script owns version bumping and tagging locally (npm run release -- <version>) and does not push, so manual package.json edits are wrong and should be reverted before running it.
Kind: procedure
Evidence:
- file: scripts/release.ts
- command: npm run release -- 0.8.0
Verdict: reject_derivable (grounded 0.84, derivable 0.84, importance 0.64, criticality 0.42)

### PI_CODING_AGENT_DIR overrides the agent directory, which allows integration tests to run the real extension tool handlers against a sandboxed registry without touching the installed environment.
Kind: pattern
Evidence:
- file: scripts/integration-test.ts — "drive registered tools through the extension wiring with a sandboxed agent dir (PI_CODING_AGENT_DIR)"
- command: npx jiti scripts/integration-test.ts
Verdict: reject_derivable (grounded 0.89, derivable 0.73, importance 0.41, criticality 0.19)

### The doctor's env-file-gitignored check was a false positive when the env key file lives outside the repository, because the repo cannot commit a path it does not contain.
Kind: gotcha
Evidence:
- command: npm run test:all
- file: src/doctor.ts
Verdict: reject_derivable (grounded 0.67, derivable 0.79, importance 0.43, criticality 0.41)

### The repo wiki's sync baseline was 54 commits stale, so a full wiki_sync would adjudicate every file-linked claim in that diff (~100–300K Jev tokens); the recommended practice is to run such a sync as its own focused session rather than alongside unrelated release work.
Kind: procedure
Verdict: reject_unsupported (grounded 0.03, derivable 0.10, importance 0.26, criticality 0.41)
